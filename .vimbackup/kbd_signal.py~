

def avg2(xs, samples, end_idx):
    _sum = [0,0]
    print ("kbdsig " + str(samples) + "    " + str(end_idx)
    for x,y in xs[end_idx-samples:end_idx]:
        _sum[0] += xs[0]
        _sum[1] += xs[1]
    _sum[0] /= (float)(samples)
    _sum[1] /= (float)(samples)
    return _sum
